# AgnecyPage-SCSS

## Clone the project:

git clone https://github.com/Vlad-Karpenko/AgnecyPage-SCSS.git

## Upload npm packages:

npm install

## Create build folder

gulp build

## Create build folder and run server:

gulp server
